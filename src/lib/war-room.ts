/** Shared Portal channel id + options — must match across every useChannel call. */
export const WAR_ROOM_ID = "war-room-alpha";

export const WAR_ROOM_CHANNEL = {
  channelId: WAR_ROOM_ID,
  history: 50 as const,
};
